class User {
  constructor(firstName, lastName, occupation, email, phoneNumber, slug) {
    this.firstName = firstName
    this.lastName = lastName
    this.occupation = occupation
    this.email = email
    this.phoneNumber = phoneNumber
    this.slug = slug
  }
}

export default User
